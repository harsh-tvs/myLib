object Versions {
    // Firebase
    const val firebase = "4.3.10"
    const val firebaseCrashlytics = "2.8.1"
    const val firebaseCrash = "17.3.0"
    const val firebase_analytics = "18.0.0"
    const val firebase_core = "18.0.0"

    // app level
    const val gradle = "7.3.0"
    const val kotlin = "1.6.10"

    // kotlin libs
    const val ktxCore = "1.6.0"
    const val ktxCoroutine = "1.5.2"

    // Android Default
    const val appcompat = "1.1.0"
    const val androidMaterial = "1.10.0"
    const val constraintLayout = "2.1.3"

    // Androidx Lifecycle
    const val androidxLifeCycle = "2.4.0"

    // Login Lib
    const val timber = "5.0.1"
    const val location = "19.0.1"
    const val playServicesBasement = "18.1.0"

    // RxJava
    const val rxJava = "2.2.21"
    const val rxJavaAndroid = "2.1.1"
    const val rxJavaKotlin = "2.4.0"

    // Bluetooth Library
    const val rxBleAndroid = "1.13.0"

    // Retrofit
    const val retrofit2 = "2.4.0"
    const val okHttp = "5.0.0-alpha.2"

    // Moshi & Gson
    const val moshi: String = "1.8.0"

    // Dagger
    const val dagger: String = "2.40.3"
    const val hilt: String = "2.44"
    const val hiltKapt: String = "2.44"
    const val hiltNavigationFrag = "1.0.0" // 2.3.3

    // Leakcanary
    const val leakcanary: String = "2.8.1"

    // Navigation graph classpath
    const val navGraph: String = "2.3.5"

    // Navigation graph UI
    const val navGraphUI: String = "2.7.7"

    // Room
    const val roomVersion: String = "2.4.2"

    // Work manager
    const val work_version = "2.7.1"

    // Test
    const val junit_version = "4.13"
    const val androidx_junit_version = "1.1.2"
    const val espresso_version = "3.3.0"

    // Multidex
    const val multidex_version = "2.0.1"

    // Glide
    const val glide_version = "4.11.0"

    // Apollo
    const val apollo_version = "3.7.3"

    // firebase
    const val firebase_bom_version = "28.4.1"
    const val firebase_messaging = "23.3.1"

    // circular progress lib version
    const val circular_progress_version = "2.0.0"

    // bottom nav bar version
    const val bottom_navbar_version = "2.2"

    // jsoup version
    const val jsoup_version = "1.13.1"
    const val cropimage_version = "2.8.0"
    const val cropimageScale_version = "1.0.3"

    // wear os version
    const val compose_version = "1.3.1"
    const val wear_compose_version = "1.3.1"
    const val play_services_wearable_version = "18.0.0"
    const val wear_apollo_version = apollo_version
    const val accompanist_version = "0.25.1"
    const val desugar_version = "1.1.5"
    const val gson_version = "2.10.1"
}
